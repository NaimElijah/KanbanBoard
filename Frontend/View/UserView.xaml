<Window x:Class="Frontend.View.UserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend.View"
        mc:Ignorable="d"
        Title="UserView" Height="450" Width="600">
    <Grid Margin="0,0,0,-36">
        <Grid.Background>
            <SolidColorBrush Color="#FFF5F5F5"  Opacity="1"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" MinHeight="90" />
        </Grid.RowDefinitions>

        <TextBlock Text="My Boards" Grid.Row="0"  Margin="0,10,0,0" FontSize="28" Foreground="#FF6B8FB4"   HorizontalAlignment="Center" FontWeight="ExtraBlack" FontStyle="Italic" Opacity="0.99" Width="153" >
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="4" Color="Gainsboro" Direction="315" ShadowDepth="5"  />
            </TextBlock.Effect>
        </TextBlock>
        <ListView x:Name="BoardListView" Margin="10,10,10,10" Grid.Row="1" Foreground="White" FontSize="16" HorizontalContentAlignment="Center" SelectionChanged ="BoardListView_SelectionChanged" FontWeight="DemiBold" BorderThickness="2" Background="#FFE0DDD6">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected,Mode=TwoWay}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Name="Logout"
        HorizontalAlignment="Left"
        VerticalAlignment="Center"
        Width="75"  
        Margin="491,0,0,0"
        Height="25"
        FontWeight="Black"
        BorderThickness="1"
        Padding="5"
        Click="LogoutButton_Click"
        FontStyle="Oblique"
        Foreground="#FF557DA3"
        SnapsToDevicePixels="True"
        BorderBrush="#AEBDCA"

        ClipToBounds="True">
            <Button.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="White" Offset="0.145"/>
                    <GradientStop Color="WhiteSmoke" Offset="0.391"/>
                    <GradientStop Color="#FF928F87" Offset="1"/>
                    <GradientStop Color="#FFFBFBFA" Offset="0.258"/>
                </LinearGradientBrush>
            </Button.Background>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8" 
                  >
                        <TextBlock Text="Logout"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"
                           FontSize="14"
                           TextAlignment="Center"
                           Padding="10,5,10,5" Margin="-1,-4,0,0"
                           FontWeight="Black"
                           
                           />

                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button Name ="Create" 
                HorizontalAlignment="Left" 
                Height="39" 
                Margin="84,8,0,2" 
                Grid.Row="2" 
                VerticalAlignment="Top" 
                Foreground="#FF576C81"
                BorderThickness="1" 
                BorderBrush="#FF576C81"
                Width="102" 
                IsDefault="True">
            <Button.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="White" Offset="0.145"/>
                    <GradientStop Color="WhiteSmoke" Offset="0.391"/>
                    <GradientStop Color="#FF928F87" Offset="1"/>
                    <GradientStop Color="#FFFBFBFA" Offset="0.258"/>
                </LinearGradientBrush>
            </Button.Background>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <TextBlock Text="Add Board"
                                    TextWrapping="Wrap"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="15"
                                    TextAlignment="Center"
                                    Padding="10,5,10,5"
                                   FontWeight="ExtraBlack"
                                    />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button Content="Button" 
                HorizontalAlignment="Left" 
                Height="39" 
                Margin="412,8,2,5" 
                Grid.Row="2" 
                VerticalAlignment="Top" 
                Width="102"/>

    </Grid>
</Window>
