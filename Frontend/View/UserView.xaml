<Window x:Class="Frontend.View.UserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend.View"
        mc:Ignorable="d"
        Title="UserView" Height="450" Width="600">
    <Grid Margin="0,0,0,-36">
        <Grid.Background>
            <SolidColorBrush Color="#FFF5F5F5"  Opacity="1"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="My Boards" Grid.Row="0"  Margin="10" FontSize="24" Foreground="#FFB26363"  HorizontalAlignment="Center" FontWeight="Bold" FontStyle="Italic" Opacity="0.9" >
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="15"/>
            </TextBlock.Effect>
        </TextBlock>
        <ListView x:Name="BoardListView" Margin="10,10,10,74" Grid.Row="1" Background="#FFB1A2A2" Foreground="White" FontSize="16" HorizontalContentAlignment="Center" SelectionChanged ="BoardListView_SelectionChanged" FontWeight="DemiBold">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected,Mode=TwoWay}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Name="Logout"
        HorizontalAlignment="Left"
        VerticalAlignment="Center"
        Width="68"  
        Margin="498,0,0,0"
        Height="25"
        FontWeight="Bold"
        BorderThickness="1"
        Padding="5"
        Click="LogoutButton_Click"
        FontStyle="Oblique"
        Foreground="#FF5B3E3E"
        SnapsToDevicePixels="True"
        ClipToBounds="True">
            <Button.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="White"/>
                    <GradientStop Color="#FFFFE0E0"/>
                    <GradientStop Color="#FF776868" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
                        <TextBlock Text="Logout"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="13"
                           TextAlignment="Center"
                           Padding="10,5,10,5"
                           />

                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button Content="Button" HorizontalAlignment="Left" Height="39" Margin="84,362,0,0" Grid.Row="1" VerticalAlignment="Top" Width="102"/>
        <Button Content="Button" HorizontalAlignment="Left" Height="39" Margin="412,362,0,0" Grid.Row="1" VerticalAlignment="Top" Width="102"/>

    </Grid>
</Window>
